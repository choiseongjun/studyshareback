package study.share.com.source.utils;

import java.util.concurrent.TimeUnit;

public class MillsConstant {

    public static long ONE_HOUR = TimeUnit.MILLISECONDS.convert(1 , TimeUnit.HOURS);
    public static long ONE_DAY = TimeUnit.MILLISECONDS.convert(1 , TimeUnit.DAYS);
    public static long ONE_WEEK = TimeUnit.MILLISECONDS.convert(7 , TimeUnit.DAYS);

}
